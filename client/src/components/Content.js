import styled from "styled-components"

// provide a styled wrapper for the whatever is to be displayed in the App
const Content = styled.div`
  position: relative;
  top: 12em;
  padding: 2em;
`

export default Content